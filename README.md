# computation-of-LR-0-items
This project implements the construction of LR(0) items and the canonical collection of LR(0) states, a key concept in Compiler Design used for building bottom-up parsers.
